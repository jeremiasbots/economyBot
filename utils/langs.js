/*const languages = require("../models/languages");

async function getUserLanguage(userId){
  const userLang = await languages.findOne({ userId: userId });

  return userLang?.language || "en";
}

module.exports = { getUserLanguage };*/
